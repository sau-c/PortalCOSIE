@model PortalCOSIE.Application.Features.Certificados.DTO.CertificadoResumenDTO

@Html.AntiForgeryToken()
<div class="card shadow-sm mb-4" id="panelMiCertificado">
    <div class="card-header">
        <h6 class="mb-0">
            <i class="fas fa-certificate me-2"></i>
            Mi certificado de firma
        </h6>
    </div>
    <div class="card-body p-4">
        @if (Model is not null)
        {
            <div class="small mb-3">
                <div class="mb-1"><strong>Sujeto:</strong> @Model.Sujeto</div>
                <div class="mb-1"><strong>Serie:</strong> @Model.NumeroSerie</div>
                <div class="mb-1">
                    <strong>Vigencia:</strong>
                    @Model.VigenteDesde.ToString("dd/MM/yyyy") — @Model.VigenteHasta.ToString("dd/MM/yyyy")
                    <span class="badge ms-1 @(Model.Vigente ? "bg-success" : "bg-danger")">
                        @(Model.Vigente ? "Vigente" : "Vencido")
                    </span>
                </div>
                <div class="text-break"><strong>Thumbprint:</strong> @Model.Thumbprint</div>
            </div>
        }
        else
        {
            <p class="text-muted small mb-3">
                Genera tu par de llaves en este equipo. El portal emitirá tu certificado; solo descargarás tu archivo <strong>.key</strong>.
            </p>
        }

        <div class="row g-2 align-items-end">
            <div class="col-md-6">
                <label class="form-label fw-semibold small">Contraseña para tu .key</label>
                <input type="password" id="certificadoContrasena" class="form-control form-control-sm" minlength="8" autocomplete="new-password" />
            </div>
            <div class="col-md-6">
                <label class="form-label fw-semibold small">Confirmar contraseña</label>
                <input type="password" id="certificadoContrasenaConfirm" class="form-control form-control-sm" minlength="8" autocomplete="new-password" />
            </div>
        </div>

        <button type="button" id="btnGenerarCertificado" class="btn btn-primary btn-sm mt-3">
            <i class="fas fa-key me-1"></i>@(Model is null ? "Generar certificado" : "Re-emitir certificado")
        </button>

        <p class="text-muted small mt-3 mb-0">
            <i class="fas fa-info-circle me-1"></i>
            Guarda tu <strong>.key</strong> en un lugar seguro. El certificado público queda en el sistema; no se descarga el .cer.
        </p>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/node-forge@1.3.1/dist/forge.min.js"></script>
<script src="~/js/efirma/cert-enrollment.js" asp-append-version="true"></script>
<script>
    document.addEventListener('DOMContentLoaded', () => {
        const btn = document.getElementById('btnGenerarCertificado');
        if (!btn) return;

        const boton = bloqueadorBoton(btn);
        btn.addEventListener('click', async () => {
            const pass = document.getElementById('certificadoContrasena')?.value ?? '';
            const confirm = document.getElementById('certificadoContrasenaConfirm')?.value ?? '';

            if (pass.length < 8) {
                showGlobalModal('error', 'La contraseña debe tener al menos 8 caracteres.');
                return;
            }
            if (pass !== confirm) {
                showGlobalModal('error', 'Las contraseñas no coinciden.');
                return;
            }

            boton.bloquear();
            try {
                await enrolarCertificadoAlumno({
                    contrasena: pass,
                    url: '@Url.Action("SolicitarCertificado", "Cuenta")'
                });
                showGlobalModal('success', 'Certificado emitido. Guarda tu archivo .key.', {
                    onClose: () => location.reload()
                });
            } catch (error) {
                showGlobalModal('error', error.message, { onClose: () => boton.restaurar() });
            }
        });
    });
</script>